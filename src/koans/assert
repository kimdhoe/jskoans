// 참과 거짓에 대해 고민해봅시다.
// assert를 통해 진실성을 검사해볼 수 있습니다.
assert(_____, '참된 값이어야 합니다')

// SEP

// 적절한 테스트 문구가 여러분을 이끌어줄 수 있습니다.
assert.notOk(_____, '거짓인 값들은 false, 0, "", null, undefined 등입니다')

// SEP

// 현실을 이해하려면 실재와 우리의 기대를 비교해보아야만 합니다.
assert.strictEqual(1 + 1, _____, '1 + 1 = 2')

// SEP

// 수와 문자열은 언제나 다릅니다.
assert.strictEqual(9 === '9', _____, '9와 "9"는 같지 않습니다')

// SEP

// 하지만 가끔은 실제로는 다른 것들을 같다고 판단하기도 합니다.
assert.strictEqual(9 == '9', _____, '9와 "9"는 같을 수도 있습니다')
